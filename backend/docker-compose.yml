# Dev compose — database is now hosted on Supabase.
# This file starts the backend app only. Set DATABASE_URL and
# SUPABASE_* env vars in your .env file before running.
services:
  app:
    build: .
    container_name: absense-backend-dev
    env_file:
      - .env
    ports:
      - "8000:8000"
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
